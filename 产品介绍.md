# 产品介绍.md — B端产品经理面试助手

> 最后更新：2026-05-20 | 当前版本：v2.0.1

---

## 一、产品基础定位

**产品背景**：B端产品经理面试竞争激烈，候选人普遍面临三大痛点：面试准备缺乏针对性、模拟面试缺少真实反馈、面试过程无系统沉淀。市面现有面试工具偏通用型，缺少垂直 B 端 PM 领域的深度能力。

**使用人群**：B端产品经理求职者（初中级 → 资深均可），五模块结构：素材库（知识库）→ 简历库 → 岗位库 → 面试准备 → 模拟面试。

**核心解决痛点**：
1. 简历/JD/项目素材散落多处，缺少统一管理
2. 岗位信息碎片化（截图/文本/网页），缺少结构化提取和存档
3. 面试准备靠零散搜索，缺少基于具体简历+岗位的定制化学习材料
4. 缺少真实面试场景的角色扮演练习和量化评估反馈

**产品价值**：通用型 B 端 PM 面试全流程 Agent，五模块闭环：素材库（知识库）→ 简历库（PDF管理）→ 岗位库（结构化JD）→ 面试准备（定制学习）→ 模拟面试（角色扮演+评估）。支持 Claude Vision 自动从 JD 图片提取结构化信息。

---

## 二、完整功能清单

### 2.1 素材库（知识库）
| 功能 | 说明 |
|------|------|
| 文件导入 | 上传参考文档、项目文档、笔记（.txt/.md/.pdf/.xlsx/.xls），LLM 自动提取 |
| 全文搜索 | 关键词精确匹配素材内容 |
| 内容查看 | 点击查看文件全文，图片支持预览 |
| 素材删除 | 单文件删除，同步清理索引和物理文件 |

注：简历和 JD 请分别上传到简历库和岗位库。

### 2.2 简历库（v2.0 新增）
| 功能 | 说明 |
|------|------|
| PDF 上传 | 仅支持 PDF 格式简历 |
| 多份并存 | 支持存储多份简历 |
| 简历预览 | 浏览器原生 iframe 嵌入 PDF 预览 |
| 自动同步 | 面试准备/模拟面试中上传的新简历自动入库 |

### 2.3 岗位库（v2.0 新增）
| 功能 | 说明 |
|------|------|
| 结构化信息 | 四必填字段：公司名称、岗位名称、工作职责、任职要求 |
| Vision 图片提取 | 上传 JD 截图 → Claude Vision → 自动填充四字段 |
| 手动录入 | 支持手动填写四字段 |
| 岗位列表 | 展开查看详情，支持删除 |
| 自动同步 | 面试准备/模拟面试中新录入的岗位自动入库 |

### 2.4 面试准备
| 功能 | 说明 |
|------|------|
| 简历选择 | 从简历库下拉选择，或上传新简历（自动同步入库） |
| 岗位选择 | 从岗位库下拉选择，或手动录入新岗位（自动同步入库） |
| 材料生成 | LLM 基于简历+JD+素材库生成定制学习材料 |
| 自由对话 | 多轮问答，基于真实素材引导答题 |

### 2.5 模拟面试
| 功能 | 说明 |
|------|------|
| 简历+岗位选择 | 同面试准备的选择机制，新内容自动同步入库 |
| 角色扮演 | LLM 扮演指定公司的 B 端产品面试官 |
| 岗位信息展示 | 面试进行中展示当前岗位的公司/岗位信息 |
| 多题型覆盖 | 行为面试/产品 Sense/估算题/策略题/执行题 |
| 追问机制 | 面试官根据回答质量深挖 1-2 轮 |
| 量化评估 | 5 维度评分 + 优势/不足/改进建议/示范回答 |
| 历史记录 | 保留全部面试对话和评估结果，支持回溯查看 |

### 2.6 已移除
- 候选人画像系统（v1.4 移除）
- Obsidian Web 集成（v1.4 移除）
- 面试追踪模块（v1.1 移除）

---

## 三、AI 技术架构

```
┌──────────────────────────────────────────────┐
│                 用户接入层                     │
│    CLI (agent.py)    │    Web (web/app.py)    │
│    Rich Terminal     │    Streamlit           │
└─────────┬──────────────────┬─────────────────┘
          │                  │
┌─────────▼──────────────────▼─────────────────┐
│              技能调度层 (scheduler.py)         │
│   意图识别 → 命令解析 → Skill 路由            │
└──────┬──────┬──────┬──────┬──────────────────┘
       │      │      │      │
  ┌────▼──┐ ┌▼───┐ ┌▼───┐
  │素材库  │ │准备 │ │模拟 │
  │Skill  │ │Skill│ │Skill│
  └───┬───┘ └──┬──┘ └──┬──┘
      │        │       │
      │   ┌────▼───────▼────┐
      │   │  Obsidian 连接器 │  ← 全链路贯通
      │   └────────────────┘
      │   └────────────────┘
      │
┌─────▼────────────────────────────────────────┐
│                核心层 (core/)                   │
│  ┌──────┐ ┌──────┐ ┌───────┐ ┌───────┐      │
│  │ LLM  │ │模型  │ │存储   │ │调度   │      │
│  │ 调用 │ │定义  │ │管理   │ │路由   │      │
│  └──┬───┘ └──────┘ └───┬───┘ └───────┘      │
│     │                  │                     │
│  DeepSeek API     本地 JSON 文件存储          │
└──────────────────────────────────────────────┘

外部依赖：
  DeepSeek API (LLM) ── httpx 直接调用
  DuckDuckGo (网络搜索) ── 临时 HTML 抓取
```

---

## 四、大模型选型说明

| 项目 | 选择 |
|------|------|
| **主模型** | DeepSeek-Chat（via DeepSeek API） |
| **温度** | 0.3（结构化提取/评估）~ 0.7（面试官角色扮演） |
| **上下文窗口** | 默认 4K tokens，素材输入截断至 15K 字符 |
| **JSON Mode** | 开启（画像生成、素材提取、面试评估） |

**选型理由**：DeepSeek API 成本低（约为 GPT-4 的 1/20）、中文能力强、支持 JSON Mode。作为个人学习项目，优先成本控制。LLM 层通过 provider 抽象设计，支持一键切换到 Anthropic Claude 或 OpenAI。

---

## 五、Skill 工具能力

### 5.1 material — 素材库管理
| 命令 | 触发条件 | 功能 | 输出 |
|------|---------|------|------|
| `material import <路径>` | 用户导入文件 | 复制文件 + LLM 提取结构化信息 + 更新索引 | 结构化预览 |
| `material list` | 查看素材 | 读取本地索引和文件系统 | 树状目录 |
| `material search <关键词>` | 搜索素材 | 精确匹配 → 失败则 LLM 语义搜索 | 搜索结果表 |
### 5.2 prep — 面试准备
| 命令 | 触发条件 | 功能 | 输出 |
|------|---------|------|------|
| `prep for <公司> <岗位>` | 设定目标 | LLM 基于素材生成定制材料 | 岗位分析+匹配度+题预测+建议 |
| `prep ask <问题>` | 面试问答 | 基于素材引导答题 | 问题分析+框架+素材参考 |
| `prep search <关键词>` | 联网搜索 | DuckDuckGo 搜索 | 搜索结果摘要 |
| `prep save-note` | 保存笔记 | 保存最近一轮问答 | 确认 → 入库 |

### 5.3 mock — 模拟面试
| 命令 | 触发条件 | 功能 |
|------|---------|------|
| `mock start <公司> <岗位>` | 开始面试 | 创建面试会话，LLM 扮演面试官出题 |
| `mock answer <回答>` | 回答问题 | 保存答案，面试官追问或出下一题 |
| `mock hint` | 请求提示 | 给答题框架和切入角度（不给答案） |
| `mock end` | 结束面试 | 保存会话，计算状态 |
| `mock review` | 查看评估 | LLM 5 维度评估 + 优势/不足/建议/示范回答 |

### 5.4 Vision JD 提取（v2.0 新增）
| 命令 | 触发条件 | 功能 |
|------|---------|------|
| 岗位库 → 上传JD图片 | 用户上传 JD 截图/照片 | Claude Vision 提取四字段结构化 JSON |
| Prompt | `prompts/jd_extract.txt` | 定义提取规则和输出格式 |

---

## 六、调用方式说明

| 调用类型 | 方式 | 鉴权 |
|---------|------|------|
| LLM 调用 | httpx → DeepSeek API (`api.deepseek.com/v1/chat/completions`) | Bearer Token（环境变量 `DEEPSEEK_API_KEY` 或 config.yaml） |
| 网络搜索 | requests → DuckDuckGo HTML 页面抓取 | 无需鉴权 |
| 本地存储 | Python json/yaml 文件读写 | 无 |
| PDF 提取 | pdfminer.six 本地库 | 无 |
| Excel 读取 | openpyxl 本地库 | 无 |
| 图片存储 | Pillow 本地库 | 无 |

---

## 七、完整工作流

### 7.1 素材库导入链路
```
用户上传文件 → 识别文件类型（简历/JD/项目/图片）
  → 图片：直接复制到 data/materials/raw/images/，记录元数据
  → Excel：openpyxl 读取转 markdown 表格 → LLM 提取结构化信息
  → 文本/PDF：read_text/pdfminer 提取 → LLM 提取结构化信息
  → 更新 data/materials/index.json 索引
  → 如需生成画像：汇总全部文本素材 → LLM 综合分析 → 保存 profile.json
```

### 7.2 面试准备链路
```
用户设定目标 → 加载候选人画像 + 素材摘要
  → LLM 分析 JD 核心要求 + 匹配度 + 高频题预测
  → 用户提问 → 构建上下文（画像+素材）
  → LLM 回答（prep_assistant prompt：引导框架而非代答）
  → 可选：搜索互联网补充信息
```

### 7.3 模拟面试链路
```
用户选择简历（素材库/本地上传）+ JD（素材库/本地上传）
  → 加载简历内容 + JD 内容（支持 txt/md/pdf/xlsx/png/jpg/webp）
  → 图片素材：读取尺寸元数据，提示当前模型不支持 OCR 文字提取
  → LLM 初始化面试官角色（interviewer prompt）
  → 面试官自我介绍+出第一题
  → 用户回答 → LLM 判断追问还是出下一题
  → 循环至 10 题或手动结束
  → LLM 5 维度评估（evaluator prompt）→ 输出报告
```

### 7.4 Obsidian 集成链路
```
配置 vault_path → 首次扫描建索引（缓存到本地）
  → material search 同步搜索 Vault + 素材库
  → prep ask 自动注入 Vault 概览到上下文
  → Web UI Obsidian 面板：搜索 + 预览 + 一键导入
```

---

## 八、提示词工程

| Prompt 文件 | 角色设定 | 关键约束 | 输出格式 |
|------------|---------|---------|---------|
| `material_extract.txt` | B 端 PM 面试素材分析师 | 保留量化数字、按文件类型提取不同字段 | JSON |
| `prep_assistant.txt` | B 端 PM 面试准备助手 | 基于真实素材、引导而非代答、指出可用项目经历 | 结构化文本 |
| `interviewer.txt` | 指定公司 B 端 PM 面试官 | 5 种题型穿插、追问 1-2 轮、保持专业不评价 | 面试官发言 |
| `evaluator.txt` | 资深 B 端 PM 面试教练 | 5 维度评分、具体反馈、示范回答 | JSON |
| `jd_extract.txt` | JD 信息提取助手 | 从图片提取公司/岗位/职责/要求 | JSON |

---

## 九、异常与边界处理

- **LLM 调用失败**：抛出 RuntimeError 并显示错误详情，不静默吞异常
- **文件不存在**：返回明确红色错误提示，提示用户检查路径
- **JSON 解析失败**：清理 markdown 包围后重试，仍失败则抛异常
- **素材库为空**：返回引导提示「请先导入素材」
- **模拟面试最大题数**：硬限制 10 题，超限自动结束并提示查看评估
- **配置缺失**：LLM provider/api_key 支持环境变量 → config.yaml 二级回退
- **token 超限**：素材文本截断至 15K-30K 字符后送入 LLM

---

## 十、迭代状态

| 项目 | 状态 |
|------|------|
| 当前版本 | v2.0 |
| CLI 模式 | 完整可用 |
| Web UI (Streamlit) | 完整可用，已部署 Streamlit Cloud，v2.0 五模块结构 |
| 简历库 | PDF 上传 + iframe 预览，多份并存 |
| 岗位库 | 结构化四字段 + Claude Vision 图片自动提取 + 手动录入 |
| Vision 提取 | Claude Vision API（`jd_extract.txt` prompt），需 `ANTHROPIC_API_KEY` |
| 语音输入 | 预留接口（streamlit-mic-recorder + Whisper API），当前未启用 |
| 素材库格式 | 支持 txt/md/pdf/xlsx/xls/png/jpg/webp |
| 已知缺陷 | 语音功能因 Streamlit Cloud 依赖安装问题暂未启用；网络搜索未接正规 API |
| 待优化方向 | 启用语音转文字、接入正规搜索 API、多维向量语义搜索 |
